# amplify
node_modules
.amplify
amplify_outputs*
amplifyconfiguration*

# Allow committing the web/prod outputs file (used by Amplify Hosting) while still
# ignoring local/sandbox outputs.
!amplify_outputs.web.json